// Use Twitch.js to get the streamer's info and display loadouts accordingly.
const twitch = window.Twitch.ext;

twitch.onAuthorized(auth => {
  // Use auth token to interact with Twitch API if needed
});
